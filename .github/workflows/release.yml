name: Prepare Release
run-name: Release ${{ inputs.release_type }} triggered by ${{ github.actor }}

on:
  workflow_dispatch:
    inputs:
      release_type:
        description: 'Release type'
        required: true
        type: choice
        options:
          - major
          - minor
          - patch

permissions:
  contents: read

jobs:
  prepare-release:
    name: Open release PR against develop
    # Reusable workflow: secure-software-engineering/actions/release/README.md
    uses: secure-software-engineering/actions/.github/workflows/maven-release-prepare.yml@f457fd685aa454b41c84be6de48c805174af5457 # extract-sootup-release-workflows
    with:
      release_type: ${{ inputs.release_type }}
      head_branch: develop
    secrets:
      release_pat: ${{ secrets.AUTO_MERGE_PAT }} # default GITHUB_TOKEN is blocked by branch protection / can't trigger downstream workflows
