module modmain { 	// allow reflective access to pkgmain, currently used in the example_jerry-mouse
	// opens pkgmain;  // note that pkgmainhidden is *not* open'ed or export'ed - but its HiddenMain class can be launched!
}
